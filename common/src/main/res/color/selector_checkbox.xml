<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true"  android:color="@color/orange_quaternary" />
    <item android:state_selected="true"  android:color="@color/orange_quaternary" />
    <!-- Estado por defecto o cuando el CheckBox no está marcado (no seleccionado) -->
    <item android:color="@color/white" />
</selector>